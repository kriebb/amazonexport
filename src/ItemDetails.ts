// Use the DOMParser available in the global scope of the browser environment
export interface ItemDetails {
  title: string;
  returnPolicy: string;
  price: string | undefined;
  productId: string;
  href: string;
  qty: string;
}
